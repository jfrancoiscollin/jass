# Jass — registre central des incidents techniques v1

Date de création : 2026-09-06

Statut : **registre opérationnel actif**.

Objectif : transformer chaque défaillance technique comprise en invariant durable, test de régression et, quand le risque dépend de données/runtime réels, en preflight obligatoire. Ce registre n'autorise aucun changement scientifique ; les seuils, cohortes, seeds, modèles et gates restent gouvernés par leurs preregistrations.

## Règle de capitalisation

Pour tout incident terminal ou quasi-terminal :

1. classer `TECHNICAL` vs `SCIENTIFIC` avant toute correction ;
2. publier la cause racine exacte, pas seulement le symptôme ;
3. exprimer la correction comme invariant de contrat ;
4. ajouter au minimum un test positif et un test fail-closed ;
5. ajouter un preflight target-data/runtime lorsque le synthétique ne peut pas couvrir le risque ;
6. conserver les side-effects scientifiques à zéro pendant la réparation ;
7. ne jamais modifier silencieusement une science gelée ;
8. après correction, relancer l'étape minimale qui prouve l'invariant avant le compute coûteux.

## Registre

> Cette table est générée depuis `TECHNICAL_INCIDENTS_V1.json`. Ne pas l'éditer à la main.

<!-- GENERATED_TECHNICAL_INCIDENT_TABLE_START -->
| ID | Job / contexte | Symptôme | Cause racine | Invariant / garde-fou durable | Preuve / couverture | Statut |
|---|---|---|---|---|---|---|
| TI-001 | 1806, dispatcher Level-3 | `exit 126` avant stage | le dispatcher exécutait directement un shell non exécutable | les stages shell sont invoqués explicitement par `/usr/bin/bash`, jamais par permission implicite | correction control #515 + rehearsal ultérieure verte | CLOSED |
| TI-002 | 1808, observabilité Level-3 | stage réel terminé mais état/résultats insuffisamment visibles dans control | divergence entre ownership `result` et `artifact` | chaque stage publie un status bridge déterministe et authentifie ses sorties requises | Jass #797 + rehearsals suivantes | CLOSED |
| TI-003 | 1823, isolation stage | environnement isolé cassait outils standards | `environment.inherit=[]` supprimait aussi `PATH`/`TMPDIR` nécessaires au runtime | `PATH` déterministe runner-owned ; `TMPDIR` runner-owned préservé, sans héritage ambiant arbitraire | Jass #798 + 1824 PASS | CLOSED |
| TI-004 | 1825, B2 recovery | `rclone.conf not found` / remote `r2` absent | capability object-store supprimée par isolation stage | R2 est une capability explicite ; seules les 5 variables `RCLONE_CONFIG_R2_*` documentées sont héritables ; secrets non déclarés interdits | `STAGE_OBJECT_STORE_CAPABILITY_INCIDENT_1825_20260906.md`, `test_run_experiment_stage_object_store.py`, Jass #800 | CLOSED |
| TI-005 | 1827, B2 target-data | `PROJECTION_BINDING_INVALID: full total must be positive` parent 1216 | producer et consumer X divergeaient sur un parent entièrement exact dont les recherches légitimes consomment 0 nœud | preflight target-data obligatoire avant bootstrap ; `full_nodes=0` autorisé uniquement avec `shadow_nodes=0`; support reste non nul au niveau cellule/global | `B2_EXACT_ZERO_COST_COMPAT_1827_20260906.md`, workflow `b2-exact-zero-cost-compat`, Jass #802/#803, 1830 PASS 4000/4000 | CLOSED |
| TI-006 | 1828, wrapper preflight v2 | stage `exit 1` avant diagnostic métier | script direct `python jobs/tools/...` sans bootstrap de racine repo dans `sys.path` | tout entrypoint Python exécutable directement doit se tester comme subprocess depuis le repo et initialiser son import path explicitement si nécessaire | Jass #804 ; 1830 PASS | CLOSED |
| TI-007 | 1829, terminal bundle B2 | `verified-historical.json` rejeté comme non-canonical | support JSON historique immuable utilisait une sérialisation legacy valide sémantiquement mais non canonique selon le consumer récent | compat de format limitée aux basenames legacy authentifiés ; bytes sources conservés ; aucune normalisation silencieuse de contenu | Jass #805 ; 1831 terminal PASS | CLOSED |
| TI-008 | 1797/1799, merge teacher B2 | king move rejeté avec `moving_king=1,promotes=1` | `Move.promotes` est un flag destination-rank, y compris pour un roi, tandis qu'un guard Python supposait l'inverse | nouveaux stages ne doivent pas reconstruire la sémantique de `promotes`; catalogue natif fait autorité ; compat legacy isolée et testée | `adaptive_sibling_b2_legacy_contract_compat.py` + tests | CLOSED |
| TI-009 | 1800/1801, publisher B2 | publisher exigeait artifact-dir vide mais wrapper y écrivait son reçu mécanique avant publication | collision ownership wrapper/publisher | `artifact_directory_contract=empty_or_runner_launch`; diagnostics mécaniques pré-publication vont dans result-dir, artefacts scientifiques restent owner du stage | 1801 PASS + contrat stage v1 | CLOSED |
| TI-010 | PR #807, B3 renderer pre-CPX | le renderer échoue avant génération sur l'anchor de schema C++ | l'adapter cherchait une chaîne JSON non échappée alors que le source C++ contient `\"...\"` dans un string literal | tout renderer source-to-source doit exercer le vrai CLI en CI et matcher les bytes/échappements exacts des anchors ; aucun fallback fuzzy/sed opportuniste | workflow `b3-real-adaptive-teacher`, tests renderer + direct CLI ; détecté avant CPX | CLOSED |
| TI-011 | 1832, B3 real adaptive parity | shard 11 abort `rc=-6`: `B2 teacher counter contract mismatch` avant tout verdict de parity | le renderer B3 remplaçait la boucle full-ladder B2 par la politique adaptative 100/60/2 mais conservait l'assertion B2 `q5=q50=q200=emitted` | B3 impose `q200<=q50<=q5<=emitted_siblings` et `engine_constructions=q5+q50+q200`; le renderer fail-closed si le contrat full-ladder B2 survit | `B3_ADAPTIVE_COUNTER_CONTRACT_INCIDENT_1832_20260906.md`, Jass #808, workflow B3 vert, control #529, rerun 1833 PASS exit 0, 4000 parents, mismatch_count=0, verdict `B3_REAL_ADAPTIVE_TEACHER_PARITY_ESTABLISHED_V1`; PR #812 | CLOSED |
| TI-012 | 1834, B3 fresh exclusion preparation | stage exit 2 after authenticated upstream fetches, before exclusion artefacts | the B3 exclusion consumer parsed the generic `fetch_result_files.py --report` output with `read_canonical_json()`, but the fetcher intentionally serializes its semantic report as indented JSON | generic authenticated fetch reports are validated semantically by identity/state fields; compact canonical-byte equality is required only for artefacts whose contract explicitly declares canonical serialization | `B3_EXCLUSION_FETCH_RECEIPT_JSON_INCIDENT_1834_20260906.md`, Jass #815; rerun 1835 attempt `20260906T134208Z-c553a572` PASS exit 0, combined_count=227317, component_overlap=0, union `b553939e8ded3ab31d121e40b2be9cfa1012168bf01835f692b59a60815d9ecb`, manifest `f734de99761b7a3ee7ddb107de3d678fa29eb7e39a11708b6a8c8bbbe700cc0c`; PR #820 | CLOSED |
| TI-013 | 1838, B3 fresh adaptive teacher | fresh teacher stage exit 2 after authenticated 1837 source retrieval, before required teacher outputs and without a scientific verdict | the fresh-teacher consumer required undeclared `selection.selected` and `selection.cell_quota` fields, while the sealed B2-derived source publication contract exposes total population as `selection.parents` and the frozen quota through the exact eight `selection.cells` counts | fresh-teacher source authentication must consume the actual sealed publication schema: `selection.parents==4000`, exactly eight cell counts all equal to 500, `forbidden_overlap==0`, and `target_blind==true`; no undeclared alias field may be invented by a consumer or its test fixture | `B3_FRESH_TEACHER_PUBLICATION_CONTRACT_INCIDENT_1838_20260906.md`; Jass #825; rerun 1841 attempt `20260906T154029Z-299779c0` PASS exit 0, 4000 parents, 38053 rows, verdict `B3_FRESH_ADAPTIVE_TEACHER_COMPLETE_V1`, renderer `a5f77f92abc7e77a8488c2c4751d71608d90cba04829a44f7c434138cb766d8f`, reference_audit_reads=0, full_ladder_backfill=false; PR #826 | CLOSED |
| TI-014 | 1846/1847, D1 WDL+listwise runtime | both stages exited 1 after about five minutes with inputs_authenticated=true, outputs_authenticated=false, no template RESULTS/logs and no scientific verdict | run_experiment_stage.py intentionally sanitizes the stage environment and does not propagate outer EXPECTED_CODE_SHA; D1 v1/v2 required ${EXPECTED_CODE_SHA:?} before installing traps, so the shell exited immediately on entry to EXECUTE | stage templates must consume only runner-owned stage variables or reconstruct defense-in-depth provenance from JASS_STAGE_SPEC; D1 must also retain the exact historical CURRENT_2M split 1800796/199204 and must never confuse bootstrap replication count 200000 with holdout rows | failed 1846 attempt 20260906T204702Z-2da8293b and 1847 attempt 20260906T211833Z-91414e25; run_experiment_stage.py build_command sanitized env; D1 v3 entrypoint + test_d1_stage_env_recovery.py; D1_STAGE_ENV_RECOVERY_1846_1847_20260906.md; PR #833; rerun 1849 attempt `20260906T222203Z-08fd187a` PASS exit 0 with authenticated outputs and terminal scientific readout | CLOSED |
| TI-015 | 1856, D3 runtime equal-node gate | stage exited 1 before the D3 equal-node start marker, with inputs authenticated, outputs unauthenticated and no scientific verdict | the stage preregistration guard searched for the non-existent literal `20,000`, while the frozen merged preregistration states the equal-node budget as `20000 nodes per move each arm` | runtime preregistration guards must match the exact merged preregistration bytes; recovery may correct only the guard/assertion plumbing, must fail closed on anchor drift, and may never edit frozen scientific constants or the preregistration | failed 1856 attempt `20260907T153211Z-9562f7e0`; PR #848; repaired rerun 1857 attempt `20260907T155344Z-fcdcd217` PASS exit 0 with authenticated outputs and full frozen equal-node execution; terminal scientific verdict `D3_RUNTIME_EQUAL_NODE_NOT_ESTABLISHED_V1`; PR #849 | CLOSED |
| TI-016 | 1858, D3 runtime terminal autopsy | read-only autopsy exited technically during authenticated source retrieval, before any scientific readout, with 0 games / 0 searches / 0 fits | the consumer generated shard ids with `seq -w 0 7`, which emits `0..7` for this range, while the sealed producer contract publishes exact directories `s00..s07` | consumers of sealed sharded publications must use the producer's exact shard identifiers; the D3 autopsy stage must use literal `00 01 02 03 04 05 06 07` for both fetch and analysis loops and must fail regression coverage if width-sensitive generation returns | failed attempt `20260907T162007Z-e42764d6`; PR #851; immutable recovery `cpx62-1860-l3-decision-math-d3-runtime-terminal-autopsy-recovery-requeue-v1` attempt `20260907T164613Z-0beb8796` PASS exit 0 with authenticated terminal autopsy publication; `D3_RUNTIME_TERMINAL_AUTOPSY_COMPLETE_V1`; classification `BROAD_EQUAL_NODE_SEARCH_EFFICIENCY_DEGRADATION`; PR #853 | CLOSED |
| TI-017 | D4 search-utility offline attempt 1861 | stage aborted during fresh-root-generation after authenticated inputs, before D4 root selection/teacher/fit, with no scientific verdict | `--gen-opening-pool` writes a `# count=...` metadata header plus exactly 30000 FEN payload rows, while the stage cardinality guard used `grep -cve '^[[:space:]]*$'` and therefore counted 30001 non-empty lines | cardinality checks on generated FEN pools must count payload rows only and ignore blank/comment metadata lines; recovery patching must apply exactly once and fail closed on source drift | failed attempt `20260907T175103Z-55416934`; generator source emits `# count=` header; recovery wrapper uses comment-aware `grep -cvE '^[[:space:]]*(#\|$)'`; regression test `jobs/tests/test_d4_root_cardinality_recovery.py`; PR #857 | MITIGATED — terminal recovery proof pending |
| TI-018 | D4 offline example-support terminal publication after 1862 | all 16 teacher shards and the example-prepare report were produced, but runner exited 4 with no scientific verdict | the frozen prepare CLI intentionally returns rc=4 for support INVALID; the stage attempted to capture it with `set +e`, but the installed Bash ERR trap still fires for a failing simple command and exits before the intended `prep_rc==4` publication branch | expected nonzero scientific/status return codes must be captured in an `if command; then ...; else rc=$?; fi` conditional or with ERR trap explicitly disabled; `set +e` alone is not a safe ERR-trap suppression mechanism | attempt `20260907T182914Z-1c779cc8` exit 4; artifacts include all 16 teacher reports, `d4-teacher-aggregate.json`, and `d4-example-prepare.json`; source prepare CLI returns 4 iff support is insufficient; recovery stage authenticates failed source and performs no new science; PR #859 | MITIGATED — terminal recovery readout pending |
| TI-019 | Scan-oracle Gate0 exploratory J1-J6 screen | stage exited 1 after J1/J2 completed and J3 emitted its 512-row TSV but before J3 report/readout | the scorer treated zero observed arm activation as a process-fatal condition; J3 had no single-reply extension event on the frozen 512-parent cohort | absence of treatment activation on a screening cohort is a scientific/support result and must not abort unrelated arms; it should be reported as not exercised/non-survivor | 1865 sealed artefacts contain J1/J2 TSV+report+readout and J3 TSV only; scorer calls assert_activation after all 512 searches and before report write; PR #863 | MITIGATED — partial recovery reuses J1/J2 and reruns J3-J6 with nonfatal activation accounting |
| TI-020 | ED1 1873 / 20260908T140143Z-a3ac3cdc; deterministic recovery 1874 / 20260908T161858Z-941faa2d | TECHNICAL: original ED1 wrapper exited before cohort authentication due to an OpenMP-contaminated CPU probe; no scientific verdict was produced by 1873. | OMP_NUM_THREADS=1 was exported before asserting nproc==16. GNU nproc observes OpenMP overrides. The repaired probe removes only OMP_NUM_THREADS and OMP_THREAD_LIMIT in its subprocess while retaining numeric caps. | Require cpx62 with 16 available CPUs without using nproc --all; preserve one-thread numeric caps, pinned clean code, ED1 data identities, labels, budgets, thresholds and information barriers. | Jass #870 merged as 941faa2df22c08b24cec45694d3277bd4407c0fc. jass-control/status/cpx62-1874-l3-ed1-partial-order-audit-openmp-recovery-v1.json: attempt 20260908T161858Z-941faa2d, completed exit 0 at 2026-09-08T16:24:13Z; label seal, ed1-readout.json and ED1_PARTIAL_ORDER_LABEL_SIGNAL_V1 published. Terminal rerun proves the unchanged resource guard and durable recovery.; PR #871 | CLOSED — terminal deterministic recovery 1874 authenticated |
| TI-021 | ED2-P0 1875 / 20260908T171140Z-bc30d685; cleanup-enabled 1876 / 20260908T182614Z-27c30b3f; authenticated diagnostic 1877 / 20260908T185207Z-e7b9320a | P0's stale-running observation was resolved by its READY terminal. The follow-up required proof that ED2 finalizers remove reproducible source/build scratch before publishing results while preserving scientific evidence. | Confirmed source-level exposure: original ED2 wrappers kept the entire work/src and work/build in the run tree copied and checked by the publisher. Its exact contribution to P0 elapsed time was not established. PR #873 implemented narrowly guarded cleanup and retained binary/log evidence. | Remove only the current attempt's reproducible source/build scratch, retain scientific data/models/seals/inputs/logs and native binary identities. Do not infer liveness from stale status, alter scientific gates or rewrite historical attempts. Numerical convergence failure is independent of cleanup correctness. | 1875 READY terminal already authenticated. Diagnostic 1877 completed exit 0 and authenticated original 1876 scratch-cleanup.json: source 39703 files/948400007 bytes and build 300 files/8114078 bytes removed, scientific_artifacts_modified=false, inputs_and_other_work_files_preserved=true. Native probe SHA256 b6f5ce3beceba1e121286ab3662447c53f2f7f062d5c6e023461dec2cfddc1f4, archive dc7ac01724b4fd6a15a02f1202ae5265b28867dab2432cbb5200fb6a90acf2c3. Central receipt docs/operations/ED2_1876_DIAGNOSTIC_RECEIPT_20260908.md.; PR #876 | CLOSED — P0 terminal and actual CPX narrow-cleanup proof authenticated by 1877 |
| TI-022 | ED2-P1 1876 / 20260908T182614Z-27c30b3f; authenticated diagnostic 1877 / 20260908T185207Z-e7b9320a; N1 numerical recovery | TECHNICAL/NUMERICAL: POINT stopped at the frozen 500-iteration optimizer limit after complete sealed TRAIN generation; no successful candidate, model seal or TEST result was produced. | Authenticated pipeline log reports optimizer did not converge: STOP: TOTAL NO. OF ITERATIONS REACHED LIMIT, last phase fit-point. This establishes exhaustion of the fixed L-BFGS-B iteration budget, not a measured underlying conditioning diagnosis or scientific negative. | Never increase limits or relax gates in the frozen P1 experiment. N1 explicitly versions the optimizer change, retains the exact convex objective/data/BASE/240 extras and all TEST/WDL gates, requires original-gradient convergence, and reuses authenticated complete TRAIN bytes without a regeneration fallback. TEST remains closed until both candidates are sealed. | Diagnostic jass-control/status/cpx62-1877-l3-ed2-1876-failure-diagnostic-v1.json completed exit 0; pipeline log SHA256 833fb7cf36eae30d4aa9e0efa20531cf75363e0e1a53114856ceecc1f9c8eecc; 9944 searches/273460000 requested nodes, no candidate/TEST artifacts. Eleven local numerical/reuse tests pass; full-repo native CI and CPX N1 terminal proof pending.; PR #875 | MITIGATED — versioned numerical solver and sealed TRAIN reuse; CPX terminal proof pending |
| TI-023 | Launch V2 deployed by Jass #877 and jass-control #576; real-host ED3 audit rehearsal 1879 / 20260908T202244Z-b074eeaf | Previously, component CI and a prose standard did not mechanically require complete target-host publication rehearsal. The new shared stage dispatcher now rejects missing admission and production without the matching successful published proof. | The original v1 spec/dispatcher lacked rehearsal admission. The new integration test additionally exposed canonical-spec versus raw-file hash confusion before any CPX launch; the corrected gate preserves both and tests actual published-byte roundtrip. | Require same-code/profile/normalized-spec/runtime proof, completed R2 manifest and marker, authenticated output readback and nonempty non-skipped regression suites. Expose bounded original-phase/type/source diagnostics without raw secrets. An audit receipt cannot authorize a learner. Do not infer fresh liveness from an unchanged running record. | Jass #877 merged b074eeafa32a578108bf30e3b329c83e956b2b24. Full-chain CI 34273484182 passed 23 top-level tests; build 34273484205 passed. Control #576 merged 96fe5b8668ab99f97c446a30cbadbc64d69c150f; CI 34274304113 passed five dispatcher/actual-queue tests. 1879 launch at 2026-09-08T20:22:48Z, attempt 20260908T202244Z-b074eeaf. docs/operations/LAUNCH_V2_ED3_DEPLOYMENT_RECEIPT_20260908.md records exact proof scope. Terminal R2 readback remains pending.; PR #878; ED4-P1: 1887/20260909T141444Z-93e2fd1f and1888/20260909T143020Z-93e2fd1f completed exit0. Original R2 publication/authentication/readback validated separately for both; production admission verified same code/profile/common spec/runtime and 33 regressions. Six pure payload hashes identical, model2e856652..., production receiptc1933640..., stage970cb30d.... docs/experiments/L3_ED4_CHOICE_SET_REAL_CANDIDATE_RESULTS_20260909.md. No heldout proof or promotion. | CLOSED — same-code real learner production admitted after complete authenticated rehearsal |
| TI-024 | ED3 1884 launch preparation on Windows; no remote failed attempt | Local launch preparation initially computed hashes from CRLF checkout files instead of the immutable LF Git blobs; local dispatcher validation caught the mismatch before publication. | Git checkout newline conversion changes raw file bytes while the V2 admission correctly authenticates the bytes executed on Linux. | Read immutable profile and rehearsal files from Git blobs; write production spec/admission as LF; compare normalized specs and validate exact proposed bytes with the existing Linux dispatcher before enqueue. | Corrected local builder; five Linux dispatcher tests PASS; control PR582 CI34314248908 PASS; 1884 attempt20260909T051901Z-0946f57d accepted same-code admission and published-prerequisite verification before selection.; ED4-P1 local preparation: ordinary Windows git archive converted the sealed spec from 1793 LF bytes to 1794 bytes with one CR. git -c core.autocrlf=false archive preserved exact Git blobs; five dispatcher tests passed in 0.317 s with proposed1887, spec33693e4e and admission1a332456. No remote attempt or fit. See ED4_P1_LAUNCH_READINESS_20260909.md. | CLOSED |
| TI-025 | ED3-T1 development, before historical launch | Full-sized synthetic calculation reached deterministic serialization but NumPy int64 parent margins were not JSON serializable. | The new readout retained NumPy scalar types in row-level parent output; scalar unit cases did not exercise full payload serialization. | Convert published scalar values to JSON-native types and require deterministic byte identity of every summary and JSONL payload through the real stage-publisher-fetch path before launch. | Local full-sized synthetic run passed; Linux CPX62 synthetic suite: 26 tests in 5.296 seconds, zero skips/errors/failures, including real stage-publisher-checked readback and missing-marker/corruption rejection. No historical ED3-T1 metrics or jobs ran during this repair. | CLOSED |
| TI-026 | ED3-T1 GitHub workflow publication | HTTPS Git push rejected a new CI workflow because the local OAuth token lacks workflow scope. | The configured git credential has repo/read:org/gist scopes; workflow creation requires a separate authorized capability. | Publish workflow files through the connected GitHub app with workflow capability, verify identical blob bytes, merge that commit, and then push the unchanged validated code. Do not remove or weaken CI to bypass the credential limit. | GitHub app created workflow commit fcba565292f151e4abea77cdae69cc273c3f6c9b; local and remote workflow blobs both 8dc766ee0ccda70d4dd116c6594c4df22c4c4961; merge 09bbcf582 pushed successfully through ordinary Git. | CLOSED |
| TI-027 | ED4-P1 local Linux synthetic validation | A newly installed WSL /tmp virtual environment was absent in a later invocation; no synthetic or real fit had started. | The cross-invocation lifetime of that /tmp path was not established. A workspace-resident environment removes that dependency; no scientific code or parameter was responsible. | Use a persistent workspace test runtime, record exact versions, and keep local synthetic validation distinct from same-code CPX launch admission. | Workspace Linux runtime Python 3.14 / NumPy 2.5.2 / SciPy 1.18.0 retained across calls; 18 focused tests and the exact 31-test launch regression profile passed. No real data/model read or fit. | CLOSED |
| TI-028 | ED4-P1 isolated local C++ validation | CMake configure rejected the reduced source archive because unrelated declared probe targets had no source files. | The local archive included only ed2_value_probe.cpp from jobs/tools, while the unchanged CMakeLists declares other jobs/tools C++ targets at configure time. | Include all declared source targets in an isolated source bundle; do not remove targets or change scientific compile flags to pass configuration. | Rebuilt bundle includes jobs/tools. Unchanged evaluator built with frozen 120-extra flags; actual native rehearsal/production fixture passed, followed by ED4_CHOICE_SET_OBJECTIVE_PREFLIGHT_COMPLETE_V1. Zero real fits. | CLOSED |
| TI-029 | ED4-P1 final local launch review; no remote attempt | The frozen protocol required at least 3 GiB free, but the stage had no explicit disk check before authenticating and downloading inputs. | The inherited stage schema checks host and CPU identity, not disk capacity. A metadata-only sizing observation does not implement a per-stage disk guard. | Before downloading any source, check free space on both result and artifact volumes; less than 3 GiB is a technical refusal with zero source reads and fits. Do not change scientific parameters. | Local Linux exact ED4 launch profile: 33 tests in 9.754 seconds, zero failures/errors/skips. Boundary tests cover insufficient space on either volume and exact-minimum admission, with mocked optimizer proving zero fits. The complete synthetic fit and publisher tests also pass. No real data or remote job.; PR #885 | CLOSED |
| TI-030 | ED4-C0A local inventory implementation review, before real metadata reads | Initial local handoff decoded nested metadata generically and omitted queue-only/catalog producers; limited fixtures could pass without validating actual envelopes. | Projection was implemented after generic decoding, and tests did not cover the complete transport/catalog contract. | Decode only schema-allowlisted scalar metadata at read time; lexically skip excluded values; authenticate every completed/failed envelope; union status and queue job names; unknown or missing producers prevent READY. | Root replaced projection and completed transport/catalog. Ten synthetic contract tests pass, including poisoned semantic decoding, malformed/duplicate JSON, actual envelope hashes and sizes, failed jobs, absent payloads and queue-only status. No real C0 metadata or payload read. | MITIGATED — synthetic complete contract passes; real inventory execution pending |
| TI-031 | Public ED4-P1 result and C0A preregistration publication | Automatic approval review rejected the proposed git publication before commit/push execution. | After repository ownership and admin rights were verified, automatic review still required explicit approval for public exposure of this exact scientific payload. | Keep the proposed payload local and reviewable; do not route the rejected publication through another tool or destination; await explicit user authorization. | Connected GitHub identity jfrancoiscollin matches jass owner; repository public and viewer admin. Exact result/preregistration publication question is pending. No publication attempt executed.; User explicitly replied Accord donné. Go. Public branch79a4b51ff pushed successfully; PR886 created. CI workflow update1d4f2aea published through GitHub and verified byte-identical to local blobdac39830. Future ordinary campaign steps retain existing user authorization. | CLOSED — exact public payload authorized and published |
| TI-032 | C0A metadata admission on CPX62, code1d4f2aea | The target-host synthetic tests passed, then catalogue projection failed before any R2 envelope was requested. | Frozen historical status cpx62-1820-l3-decision-math-b2-terminal-classified-failure-zero-placeholder-repair-v1 has host:null. The status schema incorrectly required a string for that optional field. | Permit null only in the optional status host descriptor; keep authenticated result-manifest host strict. Missing execution identity stays unknown and cannot authorize coverage or READY. | First admission report SHA256 0e0d25b32a12ee49606c5765788984c7ae40a6633e56a60556cfde82937ce72a; phasecatalog, duration0.228s, authenticated_envelopes0, all payload/target/model reads0. Field-projected scan of the full frozen status window found only this type mismatch. | MITIGATED — narrow status compatibility repair; repeated metadata admission pending |
| TI-033 | C0A admission on code111883548 after frozen catalogue projection | Literal catalogue identity rejected D4 before any R2 envelope fetch; attempt and code matched but the consumer assumed completed. | The literal1862 source published its roots/provenance before a downstream failure and its frozen outer state is failed exit2. Existing D4 consumers explicitly use expected-state failed. | Authenticate literal1862 via _FAILED with exact statefailed/exit2, attempt and code unchanged. Descriptor availability does not prove payload bytes, cardinality or scientific support. Never substitute a successful source. | Projected snapshot3ae5a398:1862 attempt20260907T182914Z-1c779cc8 code1c779cc87608a12b26432cad6a23872aeb5eabe8 statefailed exit2. Secondadmission0.429s,116catalogjobs,0R2envelopes,0payload/target/modelreads. Sol review confirms prereg did not require completed. Twelve synthetic tests pass including exact failed-literal admission and rejection of changed terminal state. | MITIGATED — exact literal terminal state implemented; repeated metadata admission pending |
| TI-034 | C0A metadata-only admission, code68ef32b8 | Metadata admission stopped at envelope_size_limit after17 authenticated envelopes; no payload or scientific target was read. | The local 8MiB transport guard was smaller than six historical inventory envelopes. Metadata-only sizing of116 sources measured maximum18121658bytes and total68041184bytes in7.077s. | Use bounded32MiB envelope transport for this fixed inventory; preserve exact metadata paths, field projections, source identities,900s cap and zero payload/target/model reads. Do not interpret descriptor sizing as full audit runtime. | Thirdadmission10.474s,116catalogjobs,17authenticatedenvelopes. Largest inventory is1868; six inventories exceed8MiB. Thirteen synthetic tests pass including measured-largest transport and rejection above32MiB. | MITIGATED — transport sized from metadata; repeated admission pending |
| TI-035 | CPX62 routing after ED4 C0A rerun 1890 was queued | 1890 remained pending with no status while cpx62 was explicitly active | state/host-filter/cpx62 was stale and still pinned to retired job cpx62-1886-l3-ed3-transfer-diagnostic-production-v1, preventing the runner from claiming 1890 | When an explicitly authorized cpx62 job is queued under a targeted host filter, the filter must match that job prefix; routing state must never silently keep an obsolete completed-job prefix | Before repair: state/host-active/cpx62='active cpx62', pending contained 1890, running was empty, and state/host-filter/cpx62 named 1886. jass-control#589 changes only the filter to cpx62-1890-l3-ed4-source-inventory-rehearsal-v1; terminal claim/publication proof pending.; PR #890 | MITIGATED — host filter corrected to 1890; runner claim and terminal proof pending |
| TI-036 | C0A metadata-only rehearsal 1889, authenticate-envelopes | 1889 failed with ValueError during authenticated envelope collection; scientific_verdict remained null | the collector incorrectly treated historical control-status host as immutable source identity and required equality with authenticated result-manifest host, although the C0A source identity contract does not include host | Authenticate job_id, attempt_id, code_sha, result_state and exit_code exactly; retain host as authenticated evidence only. Never use host drift to alter source coverage, classification, scientific inputs or verdict semantics. | 1889 attempt 20260909T164344Z-dc148a8a exited 2 in authenticate-envelopes with no scientific verdict. jass#889 removes only host from identity equality, adds field-specific identity failures and a host-drift regression; same-contract target-host rerun 1890 is now running on cpx62.; PR #891 | MITIGATED — mechanical repair merged; 1890 terminal proof pending |
| TI-037 | ED4 C0C run 1898 | C0C failed at EXECUTE after 0.095518 s before computation; authenticated stderr from diagnostic 1901 reports ModuleNotFoundError: No module named 'jobs'. | run_experiment_stage executes the registered Python stage by filesystem path with sanitized environment/no inherited PYTHONPATH, so Python places jobs/tools rather than the repository root on sys.path; ed4_c0c_exclusion_union_stage.py imported jobs.tools before bootstrapping the repository root. | Direct-path stage startup must resolve the repository package without changing any C0C scientific input or parser semantics. The frozen ED4 candidate, exclusion universe, thresholds, alpha and confirmation corpus remain unchanged; diagnosis/recovery consumes zero confirmation science. | 1898 attempt 20260909T212744Z-2bf3785b; authenticated diagnostic 1901 attempt 20260910T090616Z-f64849c0 exit 0 with stderr SHA a982220380299ccf5bac8e03d3d1b1530f6ebdb08bdede550ebddd1e1e06528c; direct-path no-PYTHONPATH regression added in jobs.tests.test_ed4_c0c_exclusion_union; runtime C0C proof pending.; PR #903 | MITIGATED — code/CI merge and immutable C0C rerun pending |
| TI-038 | CPX62 routing for ED4 C0C authenticated failure readout 1899 | 1899 remained in queue with no status and queue/running empty for more than one hour after merge, despite targeted routing prerequisites being present | CPX62 runner claim path has not successfully executed since the 1899 queue commit; GitOps evidence cannot yet distinguish an offline host, stopped/disabled five-minute runner timer, or an early runner service error before claim/status publication | Keep 1899 as the sole next diagnostic step; do not alter C0C science or parsing behavior, reroute scientific work, or consume confirmation data. Restore/observe CPX62 runner service, execute 1899 under its green V2 contract, then repair only the authenticated underlying 1898 technical cause | jass-control#598 merged 2026-09-09T22:06:21Z; at 2026-09-09T23:26Z queue contains cpx62-1899-l3-ed4-c0c-1898-failure-readout-v1, queue/running is empty, no 1899 status exists, state/host-active/cpx62 exists, and state/host-filter/cpx62 exactly matches 1899; runner-v3 timer contract is every five minutes; PR #900 | OPEN — CPX62 runner claim/service observation required; 1899 diagnostic still pending |
| TI-039 | ED4 C0C diagnostic 1900 | Exit 2 at authenticate-failed-attempt; RuntimeError in fetch_result_files.py line 118 after authenticated inventory inspection. | The diagnostic applied the generic nonempty-payload guard to runner stdout/stderr, which may legitimately be zero bytes. The same guard rejection is covered by synthetic regression; the sparse remote status does not yet identify the exact offending log or original 1898 exception. | Only the fixed runner stdout/stderr paths may be empty, and their actual remote objects still require verified existence, size and SHA256. Receipt remains nonempty; generic scientific readers and all frozen ED4 contracts remain unchanged. | 1900 attempt 20260910T080806Z-60653eb3 status; jobs/tests/test_ed4_c0c_failure_readout.py; immutable runtime proof pending.; PR #902 | MITIGATED — CI and authenticated diagnostic retry pending |
| TI-040 | ED4 C0C rerun 1902 | STAGE_FAILED:EXECUTE at parse-position-identity-only; frames end at fetch_parent -> fetch_result_files.py line 118. | fetch_parent selected the two sealed 1897 parent artifacts as bare result-root paths, but 1897 writes them under JASS_ARTEFACT_DIR and runner-v3 inventories paths relative to the run root as artefacts/<name>. The nonempty artifacts therefore could not be found under the requested bare names. | C0C must fetch the same immutable 1897 C0A/C0B bytes only through their authenticated runner inventory paths artefacts/..., preserving fixed job/attempt/code/SHA256 and all scientific semantics. | 1902 attempt 20260910T095847Z-390d1f31 frames; 1897 terminal nonempty artifact sizes; runner_v3_store inventory path contract; jobs.tests.test_ed4_c0c_exclusion_union parent-path regression; runtime proof pending.; PR #904 | MITIGATED — CI and immutable C0C rerun pending |
| TI-041 | ED4 C0C rerun 1903 / diagnostic 1906 | 1903 failed in fetch_result_files.py line 118 after the 1897 parent-path repair. Authenticated diagnostic 1906 found six zero-size candidate objects among 726 descriptors and zero descriptor drift. | C0B conservatively included structural-looking historical paths that are authenticated empty files. C0C passed every candidate to the generic nonempty payload fetcher, whose fail-closed guard correctly rejects zero-byte selections before parsing. | Every C0B descriptor must remain authenticated against its frozen runner inventory. Authenticated zero-byte objects with SHA256(empty) contribute exactly zero structural identities and must never be parsed; all nonempty payloads retain the existing verified fetch and parser semantics. | 1906 attempt 20260910T130300Z-60a74cf8: 726 candidate files examined, 6 ZERO_SIZE_AUTHENTICATED_CANDIDATE blockers, descriptor_drift_count=0, candidate_payload_reads=0; new focused regressions in jobs/tests/test_ed4_c0c_exclusion_union.py; runtime proof pending.; PR #908 | MITIGATED — CI and immutable C0C rerun pending |
| TI-042 | ED4 C0C metadata-only diagnostic 1905 | Job was claimed and failed before stage execution with PROFILE_IDENTITY. | The admission pinned the canonical JSON digest of the launch profile instead of the SHA256 of the actual profile file bytes required by launch_gate_v2. | Every V2 admission must pin the raw SHA256 of the exact profile file at the immutable code SHA; a failed launch cannot be retried under the same job ID. Scientific payloads and ED4 frozen semantics remain untouched. | 1905 attempt 20260910T115446Z-60a74cf8 status; admission profile_sha256 04a34e56…; immutable profile file SHA256 781d267b…; jass-control #607 queues corrected immutable retry 1906.; PR #907 | MITIGATED — corrected immutable retry 1906 pending CI/runtime proof |
| TI-043 | ED4 C0C V2 prospective recovery after diagnostic 1909 | Authenticated historical JNNW has count placeholder 0, 3023 complete records and 32-byte partial tail. | Historical job 0206 was interrupted before count backpatch while producer 1785 later published its documentary worktree. | V1 remains unchanged. V2 salvage is allowed only for the exact authenticated job/attempt/path/SHA/size/shape from diagnostic 1909; recover complete 38-byte records only, never decode the final 5 target bytes, discard the incomplete tail, and fail closed for every other malformed JNNW. | 1909 attempt 20260910T162746Z-35f0fd1b; explicit protocol decision 2026-09-10 to recover the 3023 complete records.; PR #913 | MITIGATED — prospective C0C V2 CI/runtime proof pending |
| TI-044 | ED4 C0C V2 1910 subsequent malformed JNNW | 1910 failed in parse-position-identity-only-v2 after the first explicitly authorized salvage, with frames returning through V1 parse_jnnw line 74. | At least one additional frozen historical JNNW is malformed; exact identity and envelope shape not yet localized. | Skip only the already-authorized 1909 object and inspect only authenticated JNNW envelope structure. Do not decode positions/targets or extend salvage policy until the next object is proven. | 1910 attempt 20260910T173938Z-755e6ae0; diagnostic implementation in this PR.; PR #914 | OPEN — next malformed JNNW localization pending |
| TI-045 | Queue placement repair and publication verification for job 1927 | The job was invisible to runner v3 because its script was outside the pending directory. | The queue script was placed at the queue root, which both the runner and the former control validation ignored. | Require the canonical pending directory, reject misplaced queue scripts in CI, and preserve the frozen execution contract. | Control PR #630; five dispatcher tests and the negative queue-placement fixture; completed execution and authenticated publication readback documented in docs/operations/ED4_1927_PUBLICATION_READBACK_20260912.md in this PR. The initial readback permission block was resolved by explicit user authorization. PR #929 | CLOSED — queue placement corrected, regression guard passed, execution and publication readback verified |
| TI-046 | ED4 C0C V5 source recovery after authenticated 1927 class diagnostic | V5 cannot accept the complete frozen malformed-file inventory: 15 of 231 rows are outside its partial-tail rule. | The V5 contract requires a count-zero JNNW with at least one complete record and tail length 1..37. The authenticated 1927 readout has 216 such rows plus 15 aligned count-zero objects with tail length zero. The aligned shape alone does not prove writer interruption. | Preserve V1-V5 and scientific gates. Prospective V6 may recover only the exact authenticated 231-object inventory partition, with separately pinned full and aligned row-set digests; decode only position bytes 0:33 and never interpret bytes 33:38. Missing pins, identity drift, duplicate/unseen objects, forbidden reads or missing runtime evidence fail closed. Total campaign job duration must remain within 45 minutes. | Authenticated 1927 publication/readback and authorized V6 R2→CPX62 metadata freeze: docs/operations/ED4_1927_PUBLICATION_READBACK_20260912.md, docs/operations/ED4_C0C_V6_AUTHENTICATED_FREEZE_20260912.json, docs/operations/ED4_C0C_V6_LOCAL_READINESS_20260912.md. Frozen full and aligned canonical pins are recorded there; no V6 real-data execution or scientific success is claimed. The historical approval-review rejection of the additional 171848-byte inventory fetch was resolved by explicit authorization. Local V6 profile evidence remains the historical 49-test Linux report; bounded same-code target-free rehearsal/readback remains pending.; PR #930 | MITIGATED — V6 canonical freeze complete; bounded same-code target-free rehearsal and authenticated readback pending |
| TI-047 | ED4 C0C V6 rehearsal 1928, attempt 20260912T121834Z-365f1d00 | No-sizing unit test entered real candidate transport on CPX62 and the stopped attempt published exit 143. | The test called stage.main after the runtime cap became configured, without patching the cap or builder. | The no-sizing test must set the cap to None and prove build_union_v6 has zero calls before asserting the exact fail-closed error. | jobs.tests.test_ed4_c0c_exclusion_union_v6 isolates the cap guard; docs/operations/ED4_C0C_V6_PREFLIGHT_REAL_IO_INCIDENT_20260912.md.; Authenticated failed 1928: exit 143; three metadata/log files reread; no stage receipt, execution evidence, or launch receipt; 136 temporary files (276979640 bytes). Position reads were not measured; scientific confirmation did not start. | MITIGATED_PENDING_NEW_REHEARSAL |
<!-- GENERATED_TECHNICAL_INCIDENT_TABLE_END -->

## Invariants transverses actifs

### A. Environnement

- aucun stage scientifique ne dépend d'un shell utilisateur implicite ;
- `PATH`, `TMPDIR` et capabilities externes sont runner-owned ou explicitement déclarées ;
- les secrets ne sont jamais hérités en bloc ;
- R2 utilise une allow-list fermée.

### B. Entrypoints

- tout outil CLI critique doit être exercé en CI via son mode d'invocation réel ;
- un test d'import module seul ne remplace pas un test `python path/to/tool.py ...` ;
- shell/Python/C++ sont lancés par un interpréteur/binaire explicite.

### C. Données et contrats

- tests synthétiques -> preflight target-data/runtime -> compute scientifique ;
- producer et consumer doivent être testés ensemble sur les vraies formes de données avant une étape coûteuse ;
- une incompatibilité de sérialisation ne peut pas être reclassée en résultat scientifique ;
- les compatibilités legacy sont isolées, byte-authenticated et interdites aux nouveaux contrats natifs.

### D. Publication / observabilité

- status, result et artifact ont des ownerships explicites ;
- un stage ne peut pas réussir si ses sorties required/nonempty ne sont pas authentifiées ;
- les failure classes doivent nommer le stage technique précis ;
- aucun retry ne doit masquer le job/attempt source.

## Politique pour B3 et la suite

À partir de B3, toute nouvelle stage doit satisfaire avant compute :

```text
CI contract
  -> CLI/subprocess rehearsal
  -> capability/environment preflight
  -> target-data admissibility quand applicable
  -> stage scientifique
```

Les incidents futurs sont enregistrés via `jobs/tools/technical_incident_register.py` ou via le bloc PR `JASS_TECHNICAL_INCIDENT`. La source canonique est `TECHNICAL_INCIDENTS_V1.json`; la table ci-dessus est régénérée automatiquement et la CI bloque toute dérive.
